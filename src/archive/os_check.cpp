// Check OS
bool unixOS;
std::string operatingSystem;

/*
    // check if conditional for the operating system and open a new process while this process is waiting
    if (//function which returns redirected stdout from other process and check for Windows)
    {
        unixOS = true;
    }
    if (//function which returns redirected stdout from other process and check for Unix like Linux or Macos)
    {
        msOperatingSystem = fa
    }
*/